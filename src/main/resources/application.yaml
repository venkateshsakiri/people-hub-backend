spring:
  application:
    name: school



  cloud:
    aws:
      region:
        static: eu-north-1

  datasource:
#    url: jdbc:mysql://${host}:${port}/${dbname}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    url: jdbc:mysql://mysqldb.chuq8600kpnx.eu-north-1.rds.amazonaws.com:3306/school?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#    username: ${username}
    username: admin
#    password: ${password}
    password: 3QeNthZXTR01HK3k8KpF
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect